To run the application locally, make sure that Python 3 is installed, along with the Flask framework (pip install Flask), the LIME package is installed (pip install lime).
Navigate to this directory in your terminal/console and type: “python 3 app.py” to run the application.
Alternatively, the application can be viewed at activelearningexperiment.pythonanywhere.com.
